# Task Queue

No executable task is currently queued.

## Next Approved Route

`Phase 10K-0: Material Intelligence Capability Gap Audit` is the next approved
phase, but its complete executable prompt has not yet been supplied. Per
`AGENTS.md`, incomplete tasks are not represented as `---TASK---` blocks and
must not be started.

Future Scope and Not Planned capabilities are never queue entries without an
explicit reviewer/user decision.
