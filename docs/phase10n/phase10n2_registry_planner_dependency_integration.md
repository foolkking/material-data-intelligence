# Phase 10N-2 Registry, Planner And Dependency Integration

Registry count becomes 56 after adding only
`structure.local_environment_polyhedra@0.1.0`. Its input schema requires periodic
Structure readiness; its dependency input port accepts exact N1 CrystalNN/VoronoiNN
table contracts. Its output port emits `phase10n2.local_environment_polyhedra.v1`.

DataProfile 2.1 remains unchanged. Eligibility distinguishes an exact existing N1 input,
an approved producible N1 dependency, ambiguous algorithm choice and unavailable input.
Explicit CrystalNN/VoronoiNN requests select that producer plus N2. An unspecified
algorithm with two equally applicable producers requires clarification.

AnalysisPlan 0.2 binds producer output to consumer input by exact port, contract,
checksum and lineage. Producer failure blocks N2. No alternate producer, latest Artifact,
filename, label or array position is substituted.
