# Service Evidence

`LOCAL_SERVICE_BACKED = UNAVAILABLE`: Docker is not installed in the local execution environment. Exact-SHA CI runs the N3 PostgreSQL/Redis/MinIO test as the 45th zero-skipped integration case and is the service authority.
