# Browser Evidence

Exact-SHA CI replays Chromium, Firefox, WebKit and Chromium 390x844 through the current browser gate. N3 focused component tests validate overlay/table/selection and inert payload behavior; CI remains final browser authority.
