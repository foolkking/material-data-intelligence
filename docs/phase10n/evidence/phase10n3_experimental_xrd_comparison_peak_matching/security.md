# Security

PASS by source/test audit: no arbitrary code, shell, filesystem, notebook, external network or dynamic module; no theory reimplementation, match-optimized detection, pattern shift, refinement or phase search; no fuzzy/latest/foreign/checksum bypass; no executable Artifact HTML/JS/iframe/URL. Secret scan is recorded separately.

`npm audit = UNAVAILABLE`: the configured npm mirror returned `404 NOT_IMPLEMENTED` for the audit endpoint.
