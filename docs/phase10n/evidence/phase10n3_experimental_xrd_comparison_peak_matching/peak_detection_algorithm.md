# Peak Detection

Locked SciPy 1.17.1 `scipy.signal.find_peaks`; detector ID `mdi.experimental_xrd_peak_detection@1.0.0`. Detection receives only the experimental series and resolved detector parameters. Matching output cannot tune detection.
