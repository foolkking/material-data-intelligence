# Decision Traceability

N2 follows the N0 authority chain: DataProfile 2.1 declares structure readiness;
Eligibility checks compatible N1 dependency availability; Planner and PlanValidator
bind the exact producer; QueueWorkerRuntime resolves and verifies the Artifact;
the registered adapter computes N2 facts; the persisted Artifact is the scientific
authority; Workspace and Report are consumers; Recipe remains declarative.
