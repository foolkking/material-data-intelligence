{
  "schemaVersion":"phase10m4.gallery-cases.v1",
  "implementation":"WorkspaceArtifactGallery.tsx",
  "metadataCap":256,
  "initialPayloadRequests":0,
  "groupingAuthority":"renderer component derived only after exact contract resolution; group order is display-only",
  "cases":[
    {"case":"metadata-first","result":"PASS","evidence":"all three desktop browsers observe 20 metadata cards before any /content request"},
    {"case":"open active Artifact","result":"PASS","evidence":"only selected contract bundle is requested and exact renderer reaches ready state"},
    {"case":"close active Artifact","result":"PASS","evidence":"request revision advances, AbortController aborts, payload state clears"},
    {"case":"safe download","result":"PASS_UNIT","evidence":"exact scope and SHA-256 are checked before object URL; filename is sanitized"},
    {"case":"legacy version","result":"PASS_BROWSER","evidence":"version 0 remains inert; no guessed renderer"},
    {"case":"HTML Artifact","result":"PASS_BROWSER","evidence":"download-only; no inline HTML execution"},
    {"case":"partial/failed panel gate","result":"IMPLEMENTED","evidence":"PRODUCED/PARTIAL permit load; all other panel states return PANEL_PAYLOAD_UNAVAILABLE"},
    {"case":"metadata cap","result":"PASS_UNIT","evidence":"256 exact records accepted; record 257 returns ARTIFACT_METADATA_CAP_EXCEEDED without rendering or truncation"}
  ],
  "browserArtifactCount":20,
  "browserMatrix":"PASS",
  "serviceAuthority":"separate service_backed.json"
}
