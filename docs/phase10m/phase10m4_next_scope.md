# Phase 10M-4 Reviewer Gate

Phase 10M-4 owns the typed Artifact Gallery and scientific viewer integration.
It may use the exact selection runtime and truthful identity/declaration matrix
implemented in M3. It must not reinterpret typed-unavailable object identities
as array indices, labels, or fuzzy matches.

Phase 10M-4 requires a new reviewer prompt after the verified M3 implementation,
completion record, and queue archive. No Phase 10M-4 executable task exists.
