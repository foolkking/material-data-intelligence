# Phase 10F-18 Browser and API Evidence

Evidence lives in `docs/phase10f/evidence/phase10f18_periodic_bond_topology/`. Live planner jobs execute `structure.viewer_3d` through QueueWorkerRuntime and persist v2 scene, manifest, summary, and recipe artifacts.

The runner opens the product flow, applies a bounded supercell, inspects a cross-boundary neighbor, highlights the stored endpoint, checks metrics, captures orthogonal/triclinic/mobile screenshots, and audits console/network behavior. Synthetic invalid cases remain tests; successful renderer evidence uses live adapter artifacts.
