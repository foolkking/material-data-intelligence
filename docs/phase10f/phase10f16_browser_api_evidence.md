# Phase 10F-16 Browser/API Evidence

The evidence runner creates persisted formal viewer jobs through Mock Planner,
QueueWorkerRuntime, Tool Registry, adapter execution, and artifact storage. Real
Chromium, Firefox, and WebKit then pick instanced atoms and measure distance,
angle, and dihedral. Chromium additionally verifies touch picking, PNG signature,
safe filename, legacy guidance, tab lifecycle, one canvas, zero external requests,
and no page/renderer errors. Evidence is under
`evidence/phase10f16_scientific_structure_inspection/`.
