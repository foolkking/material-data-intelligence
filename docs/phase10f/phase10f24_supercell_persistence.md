# Phase 10F-24 Supercell Persistence

`phase10f24.viewer_supercell_state.v1` is an inert local download containing scene identity, expansion, visibility, counts, tier, caps, warnings, and security policy. Replay validates schema, scene schema/resource identity, expansion, origin, and fixed internal-grid policy. It does not contain duplicated atoms, renderer objects, HTML, JavaScript, URLs, shaders, or callbacks. Measurement downloads now include the applied expansion.
