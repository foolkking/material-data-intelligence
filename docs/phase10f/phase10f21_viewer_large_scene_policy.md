# Viewer Large Scene Policy

Degraded mode changes display quality only. It does not truncate sites, bonds,
periodic offsets, topology, coordinates, or provenance. A refused scene does
not call the engine factory and therefore creates no canvas or graphics
context; validated JSON remains available. Existing canonical and derived hard
caps remain unchanged.
