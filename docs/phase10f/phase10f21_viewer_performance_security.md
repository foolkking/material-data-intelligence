# Viewer Performance Security

Artifacts cannot select thresholds, antialiasing, DPR, power preference,
segments, materials, shaders, modules, callbacks, or URLs. Classification runs
only on the immutable validated render model. No dependencies, telemetry,
external benchmark, artifact execution, or network resource were added.

Markers: `NO_EXTERNAL_NETWORK_REQUESTS`, `NO_SECRET_PATTERN_HITS`.
