# Viewer Keyboard Contract

When the viewer region is focused: arrows rotate; Shift+arrows pan; plus/equal
and minus zoom; zero or R resets; Escape clears selection and restores region
focus. Input, textarea, and select elements retain native key behavior. Camera
steps are fixed in application code and render on demand without animation.
