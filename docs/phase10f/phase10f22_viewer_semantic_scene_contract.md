# Viewer Semantic Scene Contract

The bounded DOM summary includes formula, canonical sites, species count,
lattice rows, canonical/cross-boundary/self-periodic bond counts, render mode,
full periodic selection identity, and warnings. It never enumerates the whole
scene. Selection changes use one polite atomic live region; camera motion and
pointer hover are not announced.
