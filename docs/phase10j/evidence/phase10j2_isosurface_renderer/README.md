# Phase 10J-2 Isosurface Renderer Evidence

The browser runner consumes the real Phase 10J-1 CHGCAR adapter artifacts through the local planner artifact content route. The renderer is application-owned Three.js with an application-owned Worker; artifact data is inert and no remote resources are allowed.
