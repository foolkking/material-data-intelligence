# Phase 10I Brillouin Zone Contract Evidence

Deterministic contract fixtures and independent NumPy/SciPy references. The generator performs no network access. No adapter, tool, browser, GPU, API job, or production-renderer evidence is claimed.

Replay with `uv run python scripts/generate_phase10i_brillouin_zone_evidence.py`.
