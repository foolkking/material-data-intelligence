# Phase 10H-4 Degenerate Modes

Only source-declared degeneracy groups are recorded. Individual source branches
and vectors remain present, but their basis is explicitly arbitrary inside the
degenerate subspace. Equal frequencies do not create authoritative groups.
Cross-source degenerate matching is defined at subspace level and remains
`DEFERRED_BY_DESIGN`; no principal-angle/projector benchmark is implemented.
