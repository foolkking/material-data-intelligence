# Phonon DOS Table

The persisted table contains deterministic total-DOS rows with grid index,
frequency, density, and imaginary/near-zero/real classification. It records
bounded truncation without changing canonical JSON.

The browser shows at most 300 rows and may add the selected projection column.
Native caption and headers provide the non-graphical representation.
