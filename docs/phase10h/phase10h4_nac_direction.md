# Phase 10H-4 NAC Direction

For Gamma modes with NAC enabled, the explicit/source direction is part of mode
identity and validation. Non-Gamma modes carry no Gamma direction. A direction
mismatch is rejected and cannot be hidden by a matching frequency. LO-TO mode
animation or directional interpolation is not implemented.
