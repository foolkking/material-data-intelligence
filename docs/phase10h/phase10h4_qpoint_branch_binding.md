# Phase 10H-4 Q-Point and Branch Binding

Q-points use reciprocal-fractional coordinates under `physics_2pi`, with exact
index and path segment context. Repeated Gamma labels remain distinct by index
and segment. Branches preserve Phase 10H source order and are never resorted by
frequency or eigenvector similarity. Frequency binding uses an application-owned
`1e-8 THz` tolerance, separate from the imaginary/near-zero classifier.
