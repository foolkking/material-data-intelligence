# Phase 10H-4 Display Amplitude

Display vectors are normalized so the maximum atom displacement norm is one,
then multiplied by a bounded angstrom amplitude. This is application display
state only. It is not a normal-coordinate amplitude, zero-point motion,
temperature-dependent RMS displacement, MD coordinate, or persisted scientific
source quantity.
