# Phase 10H-4 Imaginary Modes

Imaginary frequencies retain the established negative-real THz encoding and
their complex eigenvectors. They are labeled `static_unstable_direction`; the
contract does not take an absolute frequency or claim a stable periodic
oscillation. Any future animation needs an explicit reviewed display policy.
