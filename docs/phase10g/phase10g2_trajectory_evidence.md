# Trajectory Viewer Evidence

`evidence/phase10g2_trajectory_viewer/` is generated by `node apps/web/test/trajectory-viewer-browser-evidence.mjs`. It drives local mocked Planner/API artifact flow with canonical G-2 fixtures through Chromium, Firefox, WebKit, and mobile Chromium. It verifies dynamic navigation, playback cleanup, variable lattice, periodic picking, current-frame distance, supercell, context loss, and a valid 780-atom artifact refused before canvas allocation.

Each page has an isolated console/network audit. Rendered cases require one WebGL context, nonzero draw calls, stable geometry/material counts, and a nonblank composited-canvas pixel probe. Mobile evidence additionally asserts the 15 fps and three-frame cache policy. The complete frontend suite passes 132 tests; the backend suite passes 413 tests with 22 separately reported skips.

`uv lock --check` and the unchanged npm dependency tree pass. `npm audit` is unavailable because the configured npmmirror registry returns `404 [NOT_IMPLEMENTED]` for the audit endpoint; no clean-audit claim is made. Local PostgreSQL/Redis/MinIO integration could not run because Docker is unavailable, so the current-HEAD CI service-backed and zero-skip jobs remain mandatory closure gates.

Markers: `TRAJECTORY_VIEWER_BROWSER_SMOKE_PASS`, `TRAJECTORY_VIEWER_PLAYBACK_EVIDENCE_PASS`, `TRAJECTORY_VIEWER_MOBILE_SMOKE_PASS`, `NO_EXTERNAL_NETWORK_REQUESTS`, and `NO_SECRET_PATTERN_HITS`.
