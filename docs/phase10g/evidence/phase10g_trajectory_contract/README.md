# Phase 10G Trajectory Contract Evidence

Generated from application-owned fixtures and validators. Run:

`uv run python scripts/generate_phase10g_contract_evidence.py`

The Python canonical validator and independent TypeScript reference consume the same small fixtures. `artifact_hashes.json` inventories fixtures and evidence JSON. This directory contains no parser output, remote URL, executable artifact, private path, secret, cache, crash dump, or browser/viewer claim.

Markers: `PHASE10G_TRAJECTORY_CONTRACT_EVIDENCE_PASS`, `NO_EXTERNAL_NETWORK_REQUESTS`, `NO_SECRET_PATTERN_HITS`.
