# Trajectory Bond Policy

Default bond mode is `none`. No per-frame distance, chemical, or reactive topology inference runs. A future validated static-reference topology may update endpoint geometry across frames, but it remains PARTIAL_READY and was not added to the artifact contract in this phase.
